module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // Bundled fonts. Run `npx react-native-asset` after changing this to copy the
  // TTFs into the iOS bundle and Android assets and register them natively.
  assets: ['./src/assets/fonts/'],
};
