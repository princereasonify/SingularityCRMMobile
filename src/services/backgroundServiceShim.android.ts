/**
 * Android — re-export the real react-native-background-actions.
 * This file is picked up by Metro on Android; the .ios.ts shim is used on iOS.
 */
export { default } from 'react-native-background-actions';
